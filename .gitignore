# Rust build artifacts
**/target/

# Local env/secrets
.env
.env.*
!.env.example

# Local runtime state (Railway volume path is /data in prod, but local defaults may create ./data)
data/
**/*.sqlite
**/*.sqlite-wal
**/*.sqlite-shm

# Local Codex clone (reference-only; do not commit into template repo)
codex/

# Reference repos cloned during development (do not commit)
nanobot/
coworker/
guardrails/

# Local agent scratch/skills (do not commit)
.agent/
frontend/node_modules/*
demo/node_modules/*
**/*.tsbuildinfo
demo/output/raw/*
.DS_Store
demo/output/raw/
demo/output/raw/**
demo/output/tmp-*
demo/output/debug*
demo/output/slack_debug.png
demo/output/*-palette.png
demo/output/variants/*-palette.png

# Video files (use GIFs for README)
*.webm
*.mp4
*.mov
*.avi
*.mkv
